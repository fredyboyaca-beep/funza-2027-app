import { useEffect, useState } from 'react';
import { Bar, BarChart, CartesianGrid, Legend, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { api } from '../api/client';
import { KpiCard } from '../components/KpiCard';
export function Dashboard(){const [data,setData]=useState<any>(); useEffect(()=>{api.get('/dashboard').then(r=>setData(r.data))},[]); if(!data)return <p>Cargando...</p>; const k=data.kpis; return <section className="space-y-6"><div><h2 className="text-3xl font-bold">Dashboard ejecutivo</h2><p className="text-slate-500">Indicadores territoriales, demográficos y electorales agregados.</p></div><div className="grid md:grid-cols-3 xl:grid-cols-6 gap-4"><KpiCard title="Población total" value={k.poblacion_total}/><KpiCard title="Censo electoral" value={k.censo_electoral}/><KpiCard title="Barrios" value={k.barrios}/><KpiCard title="Veredas" value={k.veredas}/><KpiCard title="Participación" value={`${Math.round(k.participacion_historica*100)}%`}/><KpiCard title="Abstención" value={`${Math.round(k.abstencion_historica*100)}%`}/></div><div className="grid xl:grid-cols-2 gap-6"><div className="card"><h3 className="font-semibold mb-4">Pirámide poblacional</h3><ResponsiveContainer width="100%" height={300}><BarChart data={data.piramide}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="grupo"/><YAxis/><Tooltip/><Legend/><Bar dataKey="hombres"/><Bar dataKey="mujeres"/></BarChart></ResponsiveContainer></div><div className="card"><h3 className="font-semibold mb-4">Crecimiento poblacional</h3><ResponsiveContainer width="100%" height={300}><LineChart data={data.crecimiento}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="anio"/><YAxis/><Tooltip/><Line dataKey="poblacion" strokeWidth={3}/></LineChart></ResponsiveContainer></div></div></section>}
